#include <stdio.h>
#include <math.h>


int main() {

    printf("%f\n", sqrt(16));
    printf("%f\n", ceil(1.4)); //ceil() upwards to its nearest integer
    printf("%f\n", floor(1.4)); //floor() downwards to its nearest integer
    printf("%f\n", pow(4,3)); // 4^3

    return 0;
}